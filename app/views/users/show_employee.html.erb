<h5>Your are currently viewing the profile of <%= @employee.username %></h5>

<h3 class="text-capitalize"><%= "#{@employee.last_name}, #{@employee.first_name}" %></h3>

<div class="pull-right">
  <% if can? :new, @employee %>
      <%= link_to raw("<button class='btn btn-info btn-sm'  role='button'><i class='glyphicon glyphicon-edit'></i> ADD CLIENT </button>"), new_client_path %>
  <% end %>

</div>

